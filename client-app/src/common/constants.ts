//It is global variables initialized on the layout page
const baseUrl = window.BASE_URL.replace(new RegExp("[/]+$"), "");
const storeName = (window as any).STORE_NAME;
const locale = (window as any).STORE_LNG;

// Other constants can be placed here
